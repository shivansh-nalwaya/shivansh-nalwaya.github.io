import Loaders from "../loaders";
import scene from "../scene";

const Building = 2; //= await Loaders.GLTFLoader.loadAsync("/assets/models/building/scene.gltf");

// Building.scene.scale.setScalar(6);
// Building.scene.rotation.set(0, -Math.PI / 2, 0);
// Building.scene.position.set(0, 0, 13);
// Building.scene.traverse((c) => {
//   c.castShadow = true;
// });

// // scene.add(Building.scene);

// const Building2 = await Loaders.GLTFLoader.loadAsync("/assets/models/candy_shop/scene.gltf");

// Building2.scene.scale.setScalar(5);
// Building2.scene.rotation.set(0, -Math.PI / 2, 0);
// Building2.scene.position.set(35, 0, 18);
// Building2.scene.traverse((c) => {
//   c.castShadow = true;
// });

// scene.add(Building2.scene);

export default Building;
